{
  "name": "exploration-himalayan-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start-api": "cd api && npm install && npm start",
    "start-client": "cd client && npm start"
  }
}